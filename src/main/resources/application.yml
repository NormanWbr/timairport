spring:

  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/timairport_db
    password: eqc116
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  banner:
    location: banner.txt

  custom:
    prop: "salut"